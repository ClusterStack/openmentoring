public class Question2 {
  
  /*
   * You were given a task to create an endpoint for a company.
   * 
   * The project is a photo share app (eg.: Instagram)
   * 
   * How would you create an API andpoint that recieve data from the Client to
   * store the photo and link it to the user.
   * 
   * Constraints:
   * 
   * - The Photos are stored in a AWS Bucket and it's generated an ID
   * 
   * - You can choose if the database can be SQL or NoSQL.
   * 
   * - The definition of tables or documents are free.
   * 
   */

  /*
  -controller->service->
  client (Repository) (AWS)
- service -> OAuth
- Stpring (@Controller, @Service, @Component, @Repository)
- POST | v1/photos/{user_id} | body (string)
- GET | v1/photos/{photo_id} ->

  getPhotoById(final Long id)

  */

}
